<?php
	/*
	 * All'interno di questo file si trovana il codice che verra' incluso
	 * grazie alla funzione include()
	 */

	/* 
	 * La funzione stampa_nome( $nome ) e' stata gia' vista nella
	 * lezione precedente, per questo non inseriro' alcun commento 
	 */
	function stampa_nome( $nome ){
		echo $nome;
	}